useEffect(() => {
    setUserData(null);
    formDataMapRef.current = {};
    setDetailExpand({});
}, [props.selectedOrgId, props.selectedOrganizationId]);


onClick={() => {
    setUserData(null);
    formDataMapRef.current = {};
    props.createFocal();
}}
