const expandChange = (dataItem: any) => {
    const key = dataItem.ID;

    setDetailExpand(prev => {
        const isCurrentlyOpen = prev[key];

        if (isCurrentlyOpen) {
            // close current row
            return {};
        }

        // open new row and close others
        return { [key]: true };
    });
};
