import { useState } from "react";
import { Box, Button } from "@mui/material";
import CheckCircleIcon from '@mui/icons-material/CheckCircle';
import LensIcon from '@mui/icons-material/Lens';
import EditOutlinedIcon from '@mui/icons-material/EditOutlined';
import Tcds from "../tcds-home/Tcds"

import "./Overview.scss";
import { useNavigate, useOutletContext } from "react-router-dom";
import PathConstants from "../../../routes/PathConstants";
import { DocumentStateProps } from "../drawer-home/MainDrawer";
import StorageService from "../../../utils/StorageService";
import EnvDetail from "../../../utils/EnvDetail";
import ShellEventRegister from "../../../utils/ShellEventRegister";

export interface OverallDataContext {
    cclData: DocumentStateProps,
    ccnData: DocumentStateProps,
    cpData: DocumentStateProps,
    refreshDrawerData: () => Promise<void>,
}
 const navigateToCSRSearchPage = (rptId: number) => {
            const storageServiceTemp = new StorageService();
            const projRegId = storageServiceTemp.getApplicationDataByKey(`${EnvDetail.ParentAppName}-easaActiveSubMenu`, 'easamyworkspaceprojectid');
            const groupId = 19;
            const templateId = 2;
            const templateName = "Compliance Checklist";
            let dataToShare = {
                Action: 'CCL_CSR_CCN_Navigation',
                Detail: {
                    projectRegisterId: projRegId,
                    groupId,
                    templateId,
                    templateName,
                    reportId: rptId,
                }
            };
            let appDataEvent = new ShellEventRegister().getShellEvent();
            appDataEvent[`${EnvDetail.ParentAppName}_${EnvDetail.AppName}_EVENT_DATA`] = dataToShare;
            window.dispatchEvent(appDataEvent);
        };
export default function Overview() {
    const { ccnData, cclData, cpData, refreshDrawerData } = useOutletContext<OverallDataContext>();
    return (
        <>
            {
                (!ccnData.status || ccnData.status === "") ?
                    (<NoCCNExists />)
                    :
                    (
                        <div className="oss-ccl-overview-outer">
                            <div className="oss-ccl-overview-title">
                                CERT Strategy
                            </div>
                            <div className="oss-ccl-overview-cards">
                                <Cards ccn={ccnData} ccl={cclData} cp={cpData} />
                            </div>
                            <div className="oss-ccl-overview-tcds">
                                {/* Replace this with TCDS component */}
                                <Tcds />
                            </div>
                            <div className="oss-ccl-overview-as">
                                {/* <AuthoringSequence ccn={ccnData} ccl={cclData} cp={cpData} /> */}
                            </div>
                        </div>
                    )
            }
        </>

    )
}

interface CardAndAuthoringSequenceProps {
    ccn: DocumentStateProps,
    ccl: DocumentStateProps,
    cp: DocumentStateProps,
}

function Cards({ ccn, ccl, cp }: CardAndAuthoringSequenceProps) {
    let ccnProgress = "";
    let ccnSignProgress = "";
    if (ccn.status && ccn.status != "") {
        if (ccn.status.toLowerCase() != "released") {
            ccnProgress = "oss-ccl-card-progress";
            ccnSignProgress = "oss-ccl-sign-progress"
        } else {
            ccnProgress = "oss-ccl-card-completed";
            ccnSignProgress = "oss-ccl-sign-complete"
        }
    }
    let cclProgress = "";
    let cclSignProgress = "";
    if (ccl.status && ccl.status != "") {
        if (ccl.status.toLowerCase() != "released") {
            cclProgress = "oss-ccl-card-progress";
            cclSignProgress = "oss-ccl-sign-progress"
        } else {
            cclProgress = "oss-ccl-card-completed";
            cclSignProgress = "oss-ccl-sign-complete"
        }
    }
    let cpProgress = "";
    let cpSignProgress = "";
    if (cp.status && cp.status != "" && ccl.status && ccl.status.toLowerCase() == "released") {
        if (cp.status.toLowerCase() != "released") {
            cpProgress = "oss-ccl-card-progress";
            cpSignProgress = "oss-ccl-sign-progress"
        } else {
            cpProgress = "oss-ccl-card-completed";
            cpSignProgress = "oss-ccl-sign-complete"
        }
    }
    return (
        <div className="oss-ccl-ov-cards-outer">
            <div className={`oss-ccl-ccn-card ${ccnProgress}`}>
                <div className="oss-ccl-overview-card-title">
                    <span style={{ fontWeight: 'bold' }}>
                        Cert Classification
                    </span>
                    <span>
                        <Button
                            sx={{ textTransform: "capitalize", padding: 0 }}
                            onClick={() => navigateToCSRSearchPage(7663)}
                        >
                            Document Action
                        </Button>
                    </span>
                </div>
                <div className="oss-ccl-overview-progress-section">
                    <HighlightCard className={ccnSignProgress}>
                        {ccn.status ? ccn.status : 'Not Started'}
                    </HighlightCard>
                    {/* <span className={`oss-ccl-sign ${ccnSignProgress}`}>{ccn.status ? ccn.status : 'Not Started'}</span> */}
                    {
                        (ccn.signers && ccn.signerTotal) &&
                        (
                            <>
                                <span>|</span>
                                <span>{ccn.signers} / {ccn.signerTotal} signed</span>
                            </>
                        )
                    }
                    {
                        (ccn.revision) && (
                            <>
                                <span>|</span>
                                <span>Rev {ccn.revision}</span>
                            </>
                        )
                    }
                </div>
                <div className="oss-ccl-overview-text-section">
                    Complete CCN Document to start cert strategy.
                </div>
            </div>
            <div className={`oss-ccl-ccl-card ${cclProgress}`}>
                <div className="oss-ccl-overview-card-title">
                    <span style={{ fontWeight: 'bold' }}>
                        Cert Checklist
                    </span>
                    <span>
                        <Button
                            sx={{ textTransform: "capitalize", padding: 0 }}
                        >
                            Document Action
                        </Button>
                    </span>
                </div>
                <div className="oss-ccl-overview-progress-section">
                    <HighlightCard className={cclSignProgress}>
                        {ccl.status ? ccl.status : 'Not Started'}
                    </HighlightCard>
                    {
                        (ccl.signers && ccl.signerTotal) &&
                        (
                            <>
                                <span>|</span>
                                <span>{ccl.signers} / {ccl.signerTotal} signed</span>
                            </>
                        )
                    }
                    {
                        (ccl.revision) && (
                            <>
                                <span>|</span>
                                <span>Rev {ccl.revision}</span>
                            </>
                        )
                    }
                </div>
                <div className="oss-ccl-overview-text-section">
                    Available once CCN Document is released.
                </div>
            </div>
            <div className={`oss-ccl-cp-card ${cpProgress}`}>
                <div className="oss-ccl-overview-card-title">
                    <span style={{ fontWeight: 'bold' }}>
                        Cert Plan
                    </span>
                    <span>
                        <Button
                            sx={{ textTransform: "capitalize", padding: 0 }}
                        >
                            Document Action
                        </Button>
                    </span>
                </div>
                <div className="oss-ccl-overview-progress-section">
                    <HighlightCard className={cpSignProgress}>
                        {cp.status ? cp.status : 'Not Started'}
                    </HighlightCard>
                    {/* <span className={`oss-ccl-sign ${cpSignProgress}`}>{cp.status ? cp.status : 'Not Started'}</span> */}
                    {
                        (cp.signers && cp.signerTotal) &&
                        (
                            <>
                                <span>|</span>
                                <span>{cp.signers} / {cp.signerTotal} signed</span>
                            </>
                        )
                    }
                    {
                        (cp.revision) && (
                            <>
                                <span>|</span>
                                <span>Rev {cp.revision}</span>
                            </>
                        )
                    }
                </div>
                <div className="oss-ccl-overview-text-section">
                    Starts after CP DOC is completed.
                </div>
            </div>
        </div>
    )
}

function AuthoringSequence({ ccn, ccl, cp }: CardAndAuthoringSequenceProps) {
    const navigate = useNavigate();

    let ccnSignProgress = "";
    if (ccn.status && ccn.status != "") {
        if (ccn.status.toLowerCase() != "released") {
            ccnSignProgress = "progress"
        } else {
            ccnSignProgress = "complete"
        }
    }
    let docProgress = "";
    if (ccn.status && ccn.status != "" && ccn.status.toLowerCase() == "released") {
        if ((cp.status && cp.status != "" && cp.status != "released" && ccl.status?.toLowerCase() != "released")) {
            docProgress = "progress";
        } else if (ccl.status?.toLowerCase() == "released") {
            docProgress = "complete";
        }
    }
    // used for both ccl matrix and cri
    let cclMatrixProgress = "";
    if (ccn.status && ccl.status && ccn.status.toLowerCase() == "released") {
        if (ccl.status.toLowerCase() != "released") {
            cclMatrixProgress = "progress";
        } else {
            cclMatrixProgress = "complete";
        }
    }
    let cpProgress = "";
    if (cp.status && cp.status != "" && ccl.status && ccl.status.toLowerCase() == "released") {
        if (cp.status.toLowerCase() != "released") {
            cpProgress = "progress";
        } else {
            cpProgress = "complete";
        }
    }
    return (
        <div className="oss-ccl-as-outer">
            <span className="oss-ccl-overview-title" style={{ marginBottom: 16 }}>Authoring Sequeunce</span>

            <div className="oss-ccl-ccn-as">
                {
                    ccnSignProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    ccnSignProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    ccnSignProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${ccnSignProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>Cert Classification</span>
                        <HighlightCard className={`oss-ccl-sign-${ccnSignProgress}`}>
                            {(!ccn.status || ccn.status == "" ? "Not Started" : ccn.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Change Classification Note defining the scope and type of change. Must be in released state before anything else can begin.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }} onClick={() => navigate(PathConstants.CCN)}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    docProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    docProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    docProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${docProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>Description Of Change</span>
                        <HighlightCard className={`oss-ccl-sign-${docProgress}`}>
                            {(!ccl.status || ccl.status == "" ? "Not Started" : ccl.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Configure subsections and tag each with a CVE Panel, CDI and Regulation. This implicitly builds the panel/CDI list that feeds CCL Matrix and LOI.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    cclMatrixProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    cclMatrixProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    cclMatrixProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${cclMatrixProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>CCL Workstream</span>
                        <HighlightCard className={`oss-ccl-sign-${cclMatrixProgress}`}>
                            {(!ccl.status || ccl.status == "" ? "Not Started" : ccl.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Requires CP DOC to be complete - all two steps below can then be done in parallel.</span>
                    </div>
                    <div style={{ display: "flex", flexFlow: "column nowrap", gap: 16 }}>
                        <Box sx={{ boxShadow: 2 }} className="oss-ccl-subcard">
                            <div className={`oss-ccl-dot oss-ccl-dot-${cclMatrixProgress}`} />
                            <span>CCL Matrix</span>
                            <HighlightCard className={`oss-ccl-sign-${cclMatrixProgress}`}>
                                {(!ccl.status || ccl.status == "" ? "Not Started" : ccl.status)}
                            </HighlightCard>
                            <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }} onClick={() => navigate(PathConstants.CCLMATRIX)}>
                                <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                            </div>
                        </Box>
                    </div>
                    <div style={{ display: "flex", flexFlow: "column nowrap", gap: 16 }}>
                        <Box sx={{ boxShadow: 2 }} className="oss-ccl-subcard">
                            <div className={`oss-ccl-dot oss-ccl-dot-${cclMatrixProgress}`} />
                            <span>CRI Applicability</span>
                            <HighlightCard className={`oss-ccl-sign-${cclMatrixProgress}`}>
                                {(!ccl.status || ccl.status == "" ? "Not Started" : ccl.status)}
                            </HighlightCard>
                            <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }} onClick={() => navigate(PathConstants.CRI)}>
                                <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                            </div>
                        </Box>
                    </div>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    cclMatrixProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    cclMatrixProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    cclMatrixProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${cclMatrixProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>Cert Checklist</span>
                        <HighlightCard className={`oss-ccl-sign-${cclMatrixProgress}`}>
                            {(!ccl.status || ccl.status == "" ? "Not Started" : ccl.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Auto created when all workstreams are finished - then goes through its own review and release cycle.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    cpProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    cpProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    cpProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${cpProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>CP CM</span>
                        <HighlightCard className={`oss-ccl-sign-${cpProgress}`}>
                            {(!cp.status || cp.status == "" ? "Not Started" : cp.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Available after CP DOC is complete.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }} onClick={() => navigate(PathConstants.CM)}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    cpProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    cpProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    cpProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${cpProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>CP LOI</span>
                        <HighlightCard className={`oss-ccl-sign-${cpProgress}`}>
                            {(!cp.status || cp.status == "" ? "Not Started" : cp.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Available after CCL Matrix is completed.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }} onClick={() => navigate(PathConstants.LOI)}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
            <div className="oss-ccl-ccn-as">
                {
                    cpProgress == "" && (
                        <NotStartedStepper />
                    )
                }
                {
                    cpProgress == "progress" && (
                        <InProgressStepper />
                    )
                }
                {
                    cpProgress == "complete" && (
                        <CompleteStepper />
                    )
                }
                <div className={`oss-ccl-ccn-as-inner oss-ccl-as-${cpProgress}`}>
                    <div style={{ display: "inline-flex", gap: 16, alignItems: "center" }}>
                        <span style={{ fontSize: 18, fontWeight: 'bold' }}>Cert Plan</span>
                        <HighlightCard className={`oss-ccl-sign-${cpProgress}`}>
                            {(!cp.status || cp.status == "" ? "Not Started" : cp.status)}
                        </HighlightCard>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-start", alignItems: "center", gap: 8 }}>
                        <span style={{ fontSize: 14 }}>Auto created when all workstreams are finished - then goes through its own review and release cycle.</span>
                        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", cursor: 'pointer' }}>
                            <EditOutlinedIcon style={{ color: "#0078b8", fontSize: 16 }} />
                        </div>
                    </div>
                    <Button sx={{ textTransform: "capitalize", padding: 0, width: 'max-content' }}>Document Action</Button>
                </div>
            </div>
        </div>
    );
}

export function HighlightCard({ className, children }: { className?: string, children: React.ReactNode }) {
    return (
        <span className={`oss-ccl-sign ${className ?? ""}`}>
            {children}
        </span>
    );
}

function NotStartedStepper() {
    return (
        <div className="oss-ccl-stepper-outer">
            <LensIcon style={{ color: "#c8c8c8" }} />
            <div className="oss-ccl-not-stick" />
        </div>
    );
}

function InProgressStepper() {
    return (
        <div className="oss-ccl-stepper-outer">
            <LensIcon style={{ color: "#0033A1" }} />
            <div className="oss-ccl-progress-stick" />
        </div>
    );
}

function CompleteStepper() {
    return (
        <div className="oss-ccl-stepper-outer">
            <CheckCircleIcon style={{ color: "#1AA45B", backgroundColor: "white", zIndex: 2 }} />
            <div className="oss-ccl-complete-stick" />
        </div>
    );
}

function NoCCNExists() {
    
    return (
        <div className="oss-ccl-noccn-outer">
            <span style={{ fontSize: 18, fontWeight: "bold" }}>CERT Strategy</span>
            <div className="oss-ccl-noccn">
                <div className="oss-ccl-noccn-inner">
                    <span style={{ fontSize: 24, color: "#0033A1", fontWeight: "bold" }}>CCN Document Required</span>
                    <span>Certification Planning cannot begin until the CCN document is created and released</span>
                    <div style={{ display: 'flex', width: "100%", justifyContent: 'center', alignItems: 'center', marginTop: 16 }}>
                        <Button
                            variant="outlined"
                            color="inherit"
                            sx={{ textTransform: "capitalize" }}
                            className="oss-ccl-btn-primary"
                            size="small"
                           onClick={() => navigateToCSRSearchPage(7663)}
                        >
                            Create CCN Document
                        </Button>
                        <div className="oss-ccl-noccn-image">
                            Image goes here
                        </div>
                    </div>
                </div>
            </div>
        </div>
    )
}
