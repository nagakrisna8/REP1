setTimeout(() => {
  const interval = setInterval(() => {
    const btn = document.getElementById("createNewBtn");

    if (btn) {
      btn.click();
      clearInterval(interval);
    }
  }, 200);
}, 800);
