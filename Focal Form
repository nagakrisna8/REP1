const itemChange = (event: GridItemChangeEvent) => {
    const newData = focalData.map((item: any) =>
        item.ID === event.dataItem.ID
            ? { ...event.dataItem }
            : item
    );

    setFocalData(newData);
    setDirtyFlag?.(true);
};
